import * as INTERFACE from './interface';
export default class handleWebrtc {
    constructor(options: INTERFACE.GetStatusConfig);
    private getInitConfig;
    private started;
    private _janus;
    private sipcall;
    private current_jsep;
    private registered;
    private reconnect_set_time_out;
    private current_result;
    private initWebSipPhone;
    private doHangup;
}
//# sourceMappingURL=webrtc.d.ts.map